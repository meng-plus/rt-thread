from building import *

cwd  = GetCurrentDir()

CPPPATH = [cwd]

# add general drivers
src = Split('''
board.c
board_config.c
''')
if GetDepend(['BSP_USING_FAL_ON_CHIP']):
    src += ['fal_flash_hc32l19x_port.c']

group = DefineGroup('Drivers', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
